import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 80,
    width: 80,
    borderRadius: 40,
    position: 'absolute',
  },
});

export default styles;
